# Open Questions

This file records uncertainty that remained after tracing the current repository.

| Question | Files inspected | What is known | What remains unproven | Why it matters | Safest later experiment |
|---|---|---|---|---|---|
| What is the exact supported PowerShell baseline: 5.1 or 7+? | `AGENTS.md`, `payload/Scripts/UETools/UETools.psd1`, test commands | repo instructions and common commands target `pwsh`; module manifest declares `PowerShellVersion = "5.1"` | whether 5.1 is still intentionally supported for the full docs/runtime feature set | affects takeover, bug triage, and CI assumptions | run narrow docs/runtime smoke commands under 5.1 and 7 in a scratch installed repo |
| Are all domain and move operations crash-safe? | `DocsEditorApiHost.ps1`, `Test-DocsTools.ps1` | several moves have rollback-aware logic and test coverage | no single global transaction model was proven for every multi-file mutation | affects recovery guidance after partial failure | create a scratch repo and interrupt selected mutations between metadata and content writes |
| What is the exact schema for `/api/site/theme`, `/api/site/branding`, and `/api/site/overrides`? | `DocsEditorApiHost.ps1`, `SiteAdminPanel.tsx` grep summaries | route helpers exist and Site Settings uses them | this pass did not line-trace every accepted field and validation rule | affects future UI or API extensions | inspect the helper bodies directly and pair them with SiteAdminPanel request payloads |
| Is served-bundle parity fully enforced by packaging today? | installer sources, packaging tests, installed repo observations | source and installed source can match while build output drift is still a recognized risk area | whether every release/install path always refreshes `website/build/**` in practice | stale JS can invalidate source-only debugging | compare committed payload build hashes with installed build hashes after a fresh install into a scratch repo |
| What is the intended replacement for the `-WebsiteInstallMode MergeExisting` wording still present in docs theme error guidance? | `README.md`, `UEToolSuite.Docs.psm1` | current public installer switch is `-AdoptExistingWebsite`; one error string still mentions `-WebsiteInstallMode MergeExisting` | whether the older wording is obsolete or still mapped somewhere else | contradiction can mislead users during theme adoption | search the installer parameter set and narrow command help for any live `WebsiteInstallMode` support |
| How broad is route-level path traversal defense across every mutation path? | `DocsEditorApiHost.ps1`, `api.ts`, `runtimeDiscovery.ts` | path tokens and repo/docs-root validation are used throughout | this pass did not prove each helper's exact rejection strategy against crafted hostile path input | matters for security posture claims | write negative tests for `..`, absolute paths, and mixed separators against scratch API routes |
| What guarantees exist when two browser tabs author the same document? | `Save-DocsContent`, frontend authoring code | save uses `expectedHash` optimistic concurrency | whether non-content routes expose similar conflict semantics across concurrent tabs | affects future collaboration and user guidance | reproduce dual-tab saves in a scratch repo and observe API responses/logs |
| Which runtime states can `docs status` or `docs stop` mutate while diagnosing? | `Invoke-DocsStatus`, `Invoke-DocsStop` | status can prune stale entries and rewrite runtime descriptors | exact boundaries of "diagnostic only" vs cleanup behavior across all status paths | matters for incident evidence preservation | run status against a crafted stale state fixture and diff `.ue-tools/state` before/after |
